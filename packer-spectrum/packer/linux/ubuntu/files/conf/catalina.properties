# Tomcat Catalina Properties Override
# Custom properties for Spectrum deployment

# Package access control
package.access=sun.,org.apache.catalina.,org.apache.coyote.,org.apache.tomcat.,org.apache.jasper.
package.definition=sun.,java.,org.apache.catalina.,org.apache.coyote.,org.apache.tomcat.,org.apache.jasper.

# Security manager (disabled by default)
# security.manager=org.apache.catalina.security.SecurityManager

# Common classloader
common.loader="${catalina.base}/lib","${catalina.base}/lib/*.jar","${catalina.home}/lib","${catalina.home}/lib/*.jar"

# Server classloader
server.loader=

# Shared classloader
shared.loader=

# Additional properties for Spectrum
# Uncomment and customize as needed:
# spectrum.config.path=/opt/tomcat/conf/spectrum
# spectrum.log.level=INFO

